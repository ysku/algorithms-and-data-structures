PROJECT(.)

SET(LIB_SOURCE_FILES
        set.c
        set_test.c)

ADD_LIBRARY(${PROJECT_NAME} set.c)
ADD_LIBRARY(set::set ALIAS ${PROJECT_NAME})

ADD_EXECUTABLE(${PROJECT_NAME}_test ${LIB_SOURCE_FILES})
